<script setup lang="ts">
import AdminLayout from '@/Layouts/AdminLayout.vue'; import { Head, Link } from '@inertiajs/vue3';
interface Order { id: number; number: string; status_label: string; total_amount: string | number; created_at: string; user: { name: string; email: string }; payment: { method_label: string; status_label: string } | null; shipment: { status_label: string } | null; }
interface Pagination { data: Order[]; total: number; } defineProps<{ orders: Pagination }>(); const currency = new Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' });
</script>
<template><Head title="Pedidos" /><AdminLayout><div class="mb-8"><p class="text-xs font-bold uppercase tracking-[.16em] text-sky-500">Operação</p><h1 class="mt-2 text-3xl font-black tracking-tight text-slate-950">Pedidos</h1><p class="mt-2 text-sm text-slate-500">{{ orders.total }} pedido(s) registrados.</p></div><div class="overflow-x-auto rounded-2xl border border-slate-200 bg-white shadow-sm"><table class="min-w-full text-left text-sm"><thead class="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th class="px-5 py-4">Pedido</th><th class="px-5 py-4">Cliente</th><th class="px-5 py-4">Pagamento</th><th class="px-5 py-4">Envio</th><th class="px-5 py-4 text-right">Total</th></tr></thead><tbody class="divide-y divide-slate-100"><tr v-for="order in orders.data" :key="order.id"><td class="px-5 py-4"><Link :href="`/admin/orders/${order.id}`" class="font-bold text-sky-700 hover:text-sky-500">{{ order.number }}</Link><p class="mt-1 text-xs text-slate-500">{{ new Date(order.created_at).toLocaleDateString('pt-BR') }}</p></td><td class="px-5 py-4"><p class="font-semibold text-slate-800">{{ order.user.name }}</p><p class="mt-1 text-xs text-slate-500">{{ order.user.email }}</p></td><td class="px-5 py-4 text-slate-600">{{ order.payment?.method_label }}<p class="mt-1 text-xs">{{ order.payment?.status_label }}</p></td><td class="px-5 py-4 text-slate-600">{{ order.shipment?.status_label }}</td><td class="px-5 py-4 text-right font-bold text-slate-900">{{ currency.format(Number(order.total_amount)) }}</td></tr><tr v-if="!orders.data.length"><td colspan="5" class="px-5 py-12 text-center text-slate-500">Nenhum pedido registrado.</td></tr></tbody></table></div></AdminLayout></template>
