<script setup lang="ts">
import AdminLayout from '@/Layouts/AdminLayout.vue'; import { Head, Link } from '@inertiajs/vue3'; interface Item { id: number; product_name: string; variant_name: string; quantity: number; subtotal: string | number; } interface Order { number: string; status_label: string; total_amount: string | number; user: { name: string; email: string }; payment: { method_label: string; status_label: string } | null; shipment: { id: number; status_label: string; tracking_code: string | null } | null; items: Item[]; shipping_address: Record<string, string>; } defineProps<{ order: Order }>(); const currency=new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'});
</script>
<template><Head :title="order.number"/><AdminLayout><Link href="/admin/orders" class="text-sm font-bold text-sky-700">← Pedidos</Link><div class="mt-5 flex flex-wrap justify-between gap-3"><div><p class="text-xs font-bold uppercase tracking-[.16em] text-sky-500">Pedido</p><h1 class="mt-2 text-3xl font-black text-slate-950">{{ order.number }}</h1></div><strong class="text-xl text-slate-950">{{ currency.format(Number(order.total_amount)) }}</strong></div><div class="mt-7 grid gap-5 lg:grid-cols-2"><article class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 class="font-bold text-slate-900">Cliente</h2><p class="mt-3 text-sm text-slate-700">{{ order.user.name }}</p><p class="mt-1 text-sm text-slate-500">{{ order.user.email }}</p></article><article class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 class="font-bold text-slate-900">Pagamento e envio</h2><p class="mt-3 text-sm text-slate-700">{{ order.payment?.method_label }} · {{ order.payment?.status_label }}</p><p class="mt-2 text-sm text-slate-500">{{ order.shipment?.status_label }}</p><Link v-if="order.shipment" :href="`/admin/shipments/${order.shipment.id}/edit`" class="mt-4 inline-block text-sm font-bold text-sky-700">Gerenciar envio</Link></article></div><article class="mt-5 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 class="font-bold text-slate-900">Itens</h2><div v-for="item in order.items" :key="item.id" class="mt-4 flex justify-between border-t border-slate-100 pt-4"><p class="text-sm text-slate-700">{{ item.product_name }} <span class="text-slate-500">· {{ item.variant_name }} · {{ item.quantity }} un.</span></p><strong class="text-sm text-slate-900">{{ currency.format(Number(item.subtotal)) }}</strong></div></article></AdminLayout></template>
